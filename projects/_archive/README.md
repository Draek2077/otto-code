# Archived projects

Closed-out project folders, kept for the record. **Nothing here is active work.**

Do not pick items from this directory up automatically, and do not re-open an
investigation archived here — the verdict in the folder is the answer. Anything
still worth acting on was folded into `docs/` before archiving; that copy is the
live one.

Distinct from the repo's usual drain convention (fold into `docs/` and delete the
folder, as artifacts / preview-mcp / fork-review did). Folders land here instead
when the reasoning is worth keeping readable but the work is parked.

| Project                                                                             | Outcome                                                                                                                                                                                                         |
| ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [duplicate-base-workspaces](duplicate-base-workspaces/duplicate-base-workspaces.md) | Investigation closed 2026-07-24. Verdict: **prevent and steer to a worktree** — already the shipped policy. Its 3 execution gaps were fixed the same day; behaviour now lives in `docs/workspace-lifecycle.md`. |
