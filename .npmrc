; npm scripts in this repo use bash syntax (e.g. scripts/dev-home.sh).
; On Windows, npm defaults to cmd.exe for script execution regardless of
; the invoking shell, which breaks them. Force Git Bash instead.
script-shell=C:\\Program Files\\Git\\bin\\bash.exe
