name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 8 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  analyze-javascript-typescript:
    name: Analyze JavaScript and TypeScript
    runs-on: ubuntu-latest
    # The initial repository scan exceeded 20 minutes while CodeQL was still
    # analyzing the shipped TypeScript corpus. Keep a firm ceiling, but allow
    # enough time for a complete result to replace legacy Default-setup data.
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript
          build-mode: none
          config-file: ./.github/codeql-config.yml

      - name: Analyze
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:javascript-typescript
