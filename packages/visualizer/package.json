{
  "name": "@otto-code/visualizer",
  "version": "0.6.3",
  "private": true,
  "description": "Builds the Visualizer webview bundle: vendor/agent-flow's render layer + the Otto entry, compiled to a self-contained HTML shell emitted as a generated module for the app.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "vite build && node scripts/emit-bundle.mjs",
    "build:demo": "cross-env VISUALIZER_DEMO=1 vite build && node scripts/emit-bundle.mjs --demo"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@types/d3-force": "^3.0.10",
    "@types/react": "~19.1.0",
    "@types/react-dom": "~19.1.0",
    "@vitejs/plugin-react": "^6.0.1",
    "cross-env": "^7.0.3",
    "d3-force": "^3.0.0",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "tailwindcss": "^4.2.0",
    "tw-animate-css": "1.3.3",
    "typescript": "5.7.3",
    "vite": "^8.0.1"
  }
}
