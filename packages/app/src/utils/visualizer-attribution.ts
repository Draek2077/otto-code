/**
 * Third-party attribution for the Visualizer feature (docs/visualizer.md).
 *
 * Visualizer embeds the render layer of Agent Flow (vendored at
 * vendor/agent-flow/, Apache 2.0) by Simon Patole. Apache 2.0 asks that
 * significant notices be retained; this credit is how that's surfaced in
 * the app UI (Settings -> About). Quarantined here — same rationale as
 * utils/upstream-base-version.ts — so "Agent Flow" appears in exactly one
 * place instead of being typed inline in UI code (TRADEMARK.md: never use
 * it as a feature label, never ship its logos).
 */
export const VISUALIZER_UPSTREAM_NAME = "Agent Flow";
export const VISUALIZER_UPSTREAM_AUTHOR = "Simon Patole";
export const VISUALIZER_UPSTREAM_LICENSE = "Apache 2.0";
export const VISUALIZER_UPSTREAM_URL = "https://github.com/patoles/agent-flow";
