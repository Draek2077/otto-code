# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Google Play service-account key (materialized from a CI secret at submit time;
# must never be committed). See .github/workflows/android-play-release.yml.
google-service-account.json

# Written by `eas credentials -p android` -> "Download credentials from EAS to
# credentials.json" when signing a release APK locally. credentials.json carries
# the keystore passwords in plaintext, so both it and the keystore it points at
# stay out of the repo. See docs/android.md.
credentials.json
credentials/

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local
.secrets/

# typescript
*.tsbuildinfo

# vitest browser failure screenshots
.vitest-screenshots/
.vitest-attachments/

app-example

# generated native folders
/ios
/android
